import styled from 'styled-components'

export const StyledInput = styled.input`
  border: none;
  outline: none;
  background: none;
  color: #fbfbfb;
  font-style: normal;
  font-size: 16px;
  width: 220px;

  &::placeholder {
    color: #545454;
    font-style: italic;
  }
`
